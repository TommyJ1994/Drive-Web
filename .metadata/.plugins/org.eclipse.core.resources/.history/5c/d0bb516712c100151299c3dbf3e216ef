class UrlMappings {

	static mappings = {
        "/$controller/$action?/$id?(.$format)?"{
            constraints {
                // apply constraints here
            }
        }
		
		"/drive_api/drivers"(resources:"driver")

        "/"(view:"/index")
        "500"(view:'/error')
	}
}
