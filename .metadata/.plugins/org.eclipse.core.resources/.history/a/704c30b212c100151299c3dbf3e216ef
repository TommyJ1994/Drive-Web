package drive

import grails.rest.*
@Resource(uri='/drive_api/drivers')
class Driver{
	
	int age
	Date dateOfBirth;
	String country
	
    static constraints = {
		age()
		dateOfBirth()
		country()
    }
}
