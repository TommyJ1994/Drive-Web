package drive

import grails.rest.*
@Resource(uri='/api/driver', formats=['json'])

class Driver {
	
	int age
	Date dateOfBirth;
	String country
	
    static constraints = {
		age()
		dateOfBirth()
		country()
    }
}
